[build-system]
requires = [
    "setuptools>=24.2.0",
    "wheel",
    "cmake",
    "ninja",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools_scm]
version_scheme = "guess-next-dev"
local_scheme = "no-local-version"
